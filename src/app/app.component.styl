.navbar-brand
  text-shadow black;

/* Scrollable sidebar. */
.sidebar {
  position: fixed;
  top: 57px
  width: 250px;
  left: 0;
  bottom: 0;
  overflow-y: scroll;
  background: rgb(50, 56, 62);
}

.main {
  position: fixed;
  top: 3.5rem;
  left: 250px;
  right: 0;
  bottom: 0;
  overflow-y: scroll;
  padding: 1rem;
}
